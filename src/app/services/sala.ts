export interface Sala {
  salaID: number;
  numero: number;
  capacidade: number;
  disponibilidade: string;
  duracao: string;
  horaFim: string;
  horaInicial: string;
  checked: boolean;
}
